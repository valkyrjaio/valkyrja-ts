name: Restore Branch From Backup

on:
  workflow_dispatch:

run-name: "Restore ${{ github.ref_name }} from ${{ github.ref_name }}-backup"

jobs:
  restore:
    uses: valkyrjaio/.github/.github/workflows/_restore-branch-from-backup.yml@8bfa5fbef8b47c9e154c9996b17e527e6d0aee25
    permissions:
      contents: write
    secrets: inherit
