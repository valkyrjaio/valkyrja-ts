/** @type {import("prettier").Config} */
const config = {
    semi         : true,
    singleQuote  : true,
    tabWidth     : 4,
    trailingComma: 'all',
    printWidth   : 120,
};

export default config;
